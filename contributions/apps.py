"""App configuration for contributions."""

from django.apps import AppConfig


class ContributionsConfig(AppConfig):
    """Configuration for the contributions app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "contributions"
