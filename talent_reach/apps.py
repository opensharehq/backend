"""Talent reach app configuration."""

from django.apps import AppConfig


class TalentReachConfig(AppConfig):
    """Configuration for the talent_reach application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "talent_reach"
    verbose_name = "Talent Reach"
